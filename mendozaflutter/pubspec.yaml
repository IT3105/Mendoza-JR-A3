name: fitness_workout_tracker
description: A Flutter project for tracking fitness workouts

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.10.0 # Update to a stable version
  http: ^0.13.6 # Update to a stable version
  fl_chart: ^0.50.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/header_image.jpg  # Ensure the path is correct
